<%- include('./header.ejs') %>

<!-- navbar -->
<div id="navDiv">
  <h1 class="text-danger">Donor Darah</h1>
  <nav class="navbar navbar-expand-md bg-light">
    <div class="container-fluid">
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarTogglerDemo01"
        aria-controls="navbarTogglerDemo01"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="nav">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/">Beranda</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/permohonan"
              >Permohonan</a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/profil">Profil</a>
          </li>
        </ul>

        <!-- ========================= login / register =================== -->
        <% if (session === null || session === undefined){ %>
          
        </ul>
        <div class="d-flex">
          <a
            href="/account/login"
            class="btn btn-danger mx-2 rounded-pill border-danger"
            >Masuk</a
          >
          <a
            class="btn btn-light mx-2 rounded-pill border-danger text-danger"
            href="/account/register"
            >Daftar</a
          >
           <!-- ========================= end ===================== -->
        </div>
          <!-- ========================= navbar user ======================== -->
        <% } else { %>
          
          <div class="dropdown">
            <button class="btn btn-white dropdown-toggle text-secondary" type="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
             <b> Hello, <%-
              `${session.username[0].toUpperCase()}${session.username.slice(1)}`
              %></b>
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
              <li><a class="px-3" href="/permohonan/posting">Postingan</a</li>
              <li><a class="px-3" href="/account/logout">Logout</a></li>
            </ul>
          </div>
        
        <% } %> 
       

        <!-- ========================= navbar user ======================== -->
      </div>
    </div>
  </nav>
</div>